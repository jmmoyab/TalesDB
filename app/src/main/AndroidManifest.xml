<?xml version="1.0" encoding="utf-8"?>

<manifest
    xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permiso de Internet para búsqueda con APIs (v1.4.0+) -->
    <!-- INTERNET es un permiso "normal" - no requiere aprobación del usuario -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!--
        SIN PERMISOS PELIGROSOS (v1.2.2+)
        - Escribir en Download/ NO requiere permisos (Android 10+)
        - Leer archivos: Usa Storage Access Framework (selector nativo)
        - El usuario selecciona manualmente el archivo a importar/restaurar
        Los archivos se guardan en: /storage/emulated/0/Download/TalesDB/
    -->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- Actividad principal (LAUNCHER) -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action
                    android:name="android.intent.action.MAIN" />
                <category
                    android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Actividad de bienvenida -->
        <activity
            android:name=".WelcomeActivity"
            android:exported="false" />

        <!-- FileProvider para compartir archivos exportados -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>
    </application>
</manifest>